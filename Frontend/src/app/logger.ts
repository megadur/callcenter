/*
import { winston } from 'winston';

// export let winston = require('winston');
class Logger {
    log() {
    }
}
export let loginfo = {
    levels: {
        'info': 0,
        'ok': 1,
        'error': 2
    },
    colors: {
        'info': 'blue',
        'ok': 'green',
        'error': 'red'
    },
    transports: [
        new (winston.transports.Console)({ level: 'info', colorize: true })
    ]
};

export let  logger = new (winston.Logger)(loginfo);
*/
//module.exports = loginfo;
